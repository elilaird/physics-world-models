defaults:
  - _self_

name: visual_world_model
type: visual
latent_dim: 16
beta: 0.001
free_bits: 0.0
hidden_dim: 32
action_embedding_dim: 2
context_length: 3
predictor_weight: 1.0
velocity_weight: 0.0
observation_dt: ${dataset.dt}
# ODE predictor settings (ignored by non-ODE predictors)
predictor_dt: ${dataset.dt}
integration_method: rk4
damping_init: -1.0
