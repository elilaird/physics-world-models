_target_: src.models.predictors.HamiltonianLeapfrogPredictor
name: hamiltonian_leapfrog
latent_dim: ${model.latent_channels}
action_dim: ${env.action_dim}
action_embedding_dim: ${model.action_embedding_dim}
hidden_dim: 256
backbone_hidden: 128
backbone_layers: 2
dt: ${dataset.dt}
n_leapfrog_steps: 3
damping_init: -1.0
